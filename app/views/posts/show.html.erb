<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>

<br><br>
<span>Total comments: </span><strong><%= @post.comments.count %></strong>
<%= render @post.comments %>

<h3>Comments:</h3>
<%= render 'comments/fields' %>
